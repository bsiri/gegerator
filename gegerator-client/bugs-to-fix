0. TODO:
- replace all ".subscribe" on stores by "selectSignal" so I don't need to migrate the complete redux plumbing (see app.ts)


1. When the wizard mode is active, the roadmap selection is not updated in session view because Angular does not detect the changes.
2. The sort button in the Movies view ("trier") should be a toggle, because currently there is no feedback that let you know whether the sort is active or