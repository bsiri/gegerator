apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gegerator.fullname" . }}-test-connection"
  labels:
    {{- include "gegerator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gegerator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
